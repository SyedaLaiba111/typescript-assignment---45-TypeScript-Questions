const famous_person: string = "Albert Einstein";

const quoote: string = "A person who never made a mistake never tried anything new.";

const msge: string = `${famous_person} once said, "${quoote}"`;

console.log(msge);
