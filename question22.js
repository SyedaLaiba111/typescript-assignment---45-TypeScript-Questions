"use strict";
const numbers = [1, 2, 3, 4, 5];
//const indexError = numbers[10] ...this line will throug error
const indexError = numbers[0];
// Attempting to print the result (this line will throw an error)
console.log(indexError);
